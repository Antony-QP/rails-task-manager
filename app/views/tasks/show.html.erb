<ul>
  <li><%= @task.title %></li>
  <li><%= @task.details %></li>
  <li>
    <%= @task.completed %> <%= link_to "Delete #{@task.title}?",
    task_path(@task), method: :delete, data: { confirm: "Are you sure?"} %>
  </li>
</ul>
